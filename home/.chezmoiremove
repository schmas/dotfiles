.config/fish/conf.d/zzz-98-mise-config.fish
.config/fish/conf.d/01-mise-activate.fish
.config/fish/conf.d/zzz-99-pay-respects.fish
.config/fish/conf.d/71-pay-respects.fish
.config/fish/conf.d/00-pay-respects.fish
.config/fish/conf.d/02-pay-respects.fish

# Old podman->docker shim: `docker` is now the real Homebrew client (remote daemon
# via context). macOS-only — `bin/macos` is already .chezmoiignore'd on Linux.
bin/macos/docker

# Brewfile moved to .config/etc/Brewfile
Brewfile

# ClaudeKit → AgentKit migration: ck-update replaced by ak-update;
# ck-reapply-claude-naming is obsolete (AgentKit ignores .ck.json).
bin/ck-update
bin/ck-reapply-claude-naming

# AgentKit reads ~/.agentkit/config.yaml; .ck.json values are inert and make
# `ak doctor` report legacy residue. Preferences now live in .agentkit/config.yaml.
.claude/.ck.json
