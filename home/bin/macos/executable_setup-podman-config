#!/usr/bin/env bash
# Bash3 Boilerplate. Copyright (c) 2014, kvz.io

set -o errexit
set -o pipefail
set -o nounset
# set -o xtrace

echo
echo "Setup podman config"
sudo podman-mac-helper install

sudo mkdir -p /etc/containers
sudo touch /etc/containers/nodocker
