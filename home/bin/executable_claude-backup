#!/usr/bin/env bash
set -euo pipefail

cd ~/.claude

if [[ -z $(git status --porcelain) ]]; then
  echo "No changes to backup"
  exit 0
fi

git add -A
git commit -m "Backup $(date '+%Y-%m-%d %H:%M')"
git push

echo "Claude config backed up successfully"
