# [".config/nvim"]
# type = "archive"
# url = "https://github.com/schmas/nvim_lazy/archive/master.tar.gz"
# exact = false
# stripComponents = 1
# refreshPeriod = "168h"

# upall — the Go TUI updater. Installed once via
# .chezmoiscripts/01-common/run_once_after_05.5-upall-install.sh.tmpl;
# it self-updates after that, so no chezmoiexternal refresh is needed.
# The v2 bash script lives on as `upall-classic`
# (home/bin/executable_upall-classic.tmpl).
