README.md
docs/
etc/
__*

# Reference snapshot of the app-managed ~/.supacode/settings.json — versioned
# in dotfiles for bootstrap/restore, but not applied (Supacode owns the live file).
.supacode/settings.reference.json

{{ if ne .chezmoi.os "darwin" }}
bin/macos
bin/setup-pam-watchid
.config/karabiner/assets/complex_modifications
Library
{{ end }}

{{ if ne .chezmoi.os "linux" }}
bin/linux
{{ end }}

{{ if not (lookPath "claude") }}
.claude/skills/aaa-db
{{ end }}

# television cable channels: `tv update-channels` owns the upstream defaults,
# so chezmoi tracks only the channels we customized or authored. To track a new
# custom channel, add a matching `!` un-ignore line below.
.config/television/cable/*.toml
!.config/television/cable/x-commands.toml
!.config/television/cable/x-shortcuts.toml
!.config/television/cable/x-zmx-sessions.toml
!.config/television/cable/x-git-branch.toml
!.config/television/cable/x-dirs.toml
!.config/television/cable/x-files.toml
