# Phase 2: Expand 10-shared.env with Centralized Vars

## Context
- Parent: [plan.md](plan.md)
- Depends on: Phase 1 (tokens removed from this file)

## Overview
- **Priority**: High (must complete before Phase 5)
- **Status**: pending
- **Description**: Add 11 literal-value env vars currently duplicated across Fish/Zsh/Bash into centralized `10-shared.env.tmpl`.

## Key Insights
- Only literal values work in centralized .env (fish parser does string matching)
- `$HOME` expansion handled by Phase 3 (fish enhancement)
- Fixes bash `VISUAL=vim` → `nvim` inconsistency
- `SYSTEMD_EDITOR` hardcoded to `nvim` (can't reference `$EDITOR` in .env for fish)

## Requirements
- All 11 vars must be literal KEY=VALUE format
- No chezmoi template syntax needed (these are static values)
- PODMAN_COMPOSE_WARNING_LOGS remains (from Phase 1)

## Related Code Files
- **Modify**: `home/dot_config/private_env/10-shared.env.tmpl`

## Implementation Steps

1. Rewrite `home/dot_config/private_env/10-shared.env.tmpl` to:
```
# Shared environment variables — generated by chezmoi, do not edit
# Add new shared vars here instead of per-shell configs

# Locale
LANG=en_US.UTF-8
LANGUAGE=en_US:en
LC_TIME=en_US.UTF-8

# Editor
VISUAL=nvim
EDITOR=nvim
SYSTEMD_EDITOR=nvim

# Paths
DOTFILES_BIN=$HOME/bin

# Misc
OPENCV_LOG_LEVEL=ERROR
CLICOLOR=1
PODMAN_COMPOSE_WARNING_LOGS=false

# Tool config
forgit_ignore=/dev/null
ASDF_NODEJS_LEGACY_FILE_DYNAMIC_STRATEGY=latest_installed
```

## Todo
- [ ] Replace contents of `10-shared.env.tmpl` (tokens already moved to 15-services)
- [ ] Verify no chezmoi template syntax needed (all literal values)
- [ ] Confirm `$HOME` in DOTFILES_BIN will work after Phase 3

## Success Criteria
- File contains 11 new vars + PODMAN_COMPOSE_WARNING_LOGS
- No `onepasswordRead` calls remain
- `$HOME` used literally (expanded by shell loaders + enhanced fish parser)

## Risk Assessment
- **Low**: `$HOME` expansion depends on Phase 3 — fish source_posix_env must be enhanced first
- If Phase 3 incomplete, DOTFILES_BIN won't resolve in fish (but fish is only shell that uses it)
