# Phase 1: Split Tokens into 15-services.env

## Context
- Parent: [plan.md](plan.md)
- Brainstorm: [brainstorm report](../reports/brainstorm-260210-1346-env-cleanup-phase2.md)

## Overview
- **Priority**: High (must complete before Phase 5)
- **Status**: pending
- **Description**: Extract API tokens from `10-shared.env.tmpl` into dedicated `15-services.env.tmpl`. Neutral filename avoids advertising secrets.

## Key Insights
- Tokens use `onepasswordRead` chezmoi function — file must remain `.tmpl`
- PODMAN_COMPOSE_WARNING_LOGS stays in 10-shared (not a secret)
- 15 prefix loads after shared config, before OS-specific

## Requirements
- Separate secrets from config vars
- No functional change — same vars, just different file
- Filename must not reveal token/secret nature

## Related Code Files
- **Create**: `home/dot_config/private_env/15-services.env.tmpl`
- **Modify**: `home/dot_config/private_env/10-shared.env.tmpl`

## Implementation Steps

1. Create `home/dot_config/private_env/15-services.env.tmpl`:
```
# Service integrations — generated by chezmoi, do not edit
MISE_GITHUB_TOKEN={{ onepasswordRead "op://Dotfiles/lzlakqn35xiz2wptj56mhtghdy/Tokens/mise" | quote }}
GITHUB_TOKEN={{ onepasswordRead "op://Dotfiles/lzlakqn35xiz2wptj56mhtghdy/Tokens/mise" | quote }}
HOMEBREW_GITHUB_API_TOKEN={{ onepasswordRead "op://Dotfiles/lzlakqn35xiz2wptj56mhtghdy/Tokens/homebrew" | quote }}
GEMINI_API_KEY={{ onepasswordRead "op://Dotfiles/mnplwh5vf7jxt3ihqu6rtjbshu/api key claude_code" | quote }}
```

2. Edit `home/dot_config/private_env/10-shared.env.tmpl`:
   - Remove the 4 token lines (MISE_GITHUB_TOKEN, GITHUB_TOKEN, HOMEBREW_GITHUB_API_TOKEN, GEMINI_API_KEY)
   - Keep header comment and PODMAN_COMPOSE_WARNING_LOGS

## Todo
- [ ] Create `15-services.env.tmpl` with 4 token vars
- [ ] Remove token lines from `10-shared.env.tmpl`
- [ ] Verify both files have correct chezmoi template syntax

## Success Criteria
- `chezmoi execute-template < 15-services.env.tmpl` outputs valid KEY=VALUE
- `10-shared.env.tmpl` no longer contains any `onepasswordRead` calls
- All 4 tokens still available via `~/.config/env/15-services.env`

## Risk Assessment
- **Low**: Simple file split, no logic change

## Security Considerations
- Filename `15-services.env` does not reveal secret content
- File inherits `private_env` directory permissions
